name: CI

on:
  push:
    branches: [main]
    paths-ignore: ['README.md']
  pull_request:
    branches: [main]
    paths-ignore: ['README.md']
  workflow_dispatch:
  # Invoked as a gate by docker-publish.yml / release.yml (`uses: ./.github/workflows/ci.yml`).
  workflow_call:

permissions:
  contents: read

jobs:
  test:
    name: Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.13"]

    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run tests
        run: uv run pytest -q

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Check formatting
        run: uv run ruff format --check .

      - name: Check linting
        run: uv run ruff check .

      - name: Type check (mypy)
        run: uv run mypy vgi_causal/ causal_worker.py

      - name: Docstring lint (pydoclint)
        run: uvx pydoclint --config pyproject.toml vgi_causal/ causal_worker.py

  metadata-quality:
    name: Metadata quality (vgi-lint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Set up Python 3.13
        run: uv python install 3.13

      - name: Install the worker (with the http extra)
        # Syncing the env installs the worker into `.venv` (with its console
        # scripts) so the linter can spawn it and introspect its catalog
        # metadata. We lint via the venv console script rather than
        # `uv run causal_worker.py`: the PEP 723 script env resolves its own
        # (potentially stale) SDK copy, which can emit an old catalog schema and
        # trip a false schema-mismatch; the venv runs the pinned SDK.
        run: uv sync --python 3.13 --extra http

      - name: vgi-lint
        uses: Query-farm/vgi-lint-check@v1
        with:
          location: ".venv/bin/vgi-causal"
          fail-on: info
          version: "0.73.0"

  # Resolve the latest published haybarn release once, so the whole matrix tests
  # the same version (and we never hardcode/pin it).
  resolve-haybarn:
    name: Resolve haybarn release
    runs-on: ubuntu-latest
    outputs:
      release: ${{ steps.r.outputs.release }}
    steps:
      - id: r
        run: |
          REL=$(gh release view --repo Query-farm-haybarn/haybarn --json tagName --jq .tagName)
          echo "release=$REL" >> "$GITHUB_OUTPUT"
          echo "Latest haybarn release: $REL"
        env:
          GH_TOKEN: ${{ github.token }}

  integration:
    name: SQL E2E (${{ matrix.transport }}) on ${{ matrix.os }}
    needs: resolve-haybarn
    strategy:
      fail-fast: false
      matrix:
        # Run the SAME sqllogictest suite over every VGI transport. The vgi
        # extension picks the transport from the ATTACH LOCATION string that
        # run-integration.sh builds per $TRANSPORT:
        #   subprocess : `uv run causal_worker.py`     (stdio; extension spawns it)
        #   http       : `http://127.0.0.1:<port>`     (worker booted with --http)
        #   unix       : `unix:///tmp/causal.sock`      (worker booted with --unix)
        os: [ubuntu-latest, macos-latest]
        transport: [subprocess, http, unix]
        include:
          - { os: ubuntu-latest, asset: haybarn_unittest-linux-amd64.zip }
          - { os: macos-latest,  asset: haybarn_unittest-osx-arm64.zip }
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Set up Python 3.13
        run: uv python install 3.13

      - name: Install the worker (from the lockfile, with the http extra)
        # The `http` extra pulls in waitress so the worker can serve `--http`.
        # Harmless for the subprocess/unix legs; required for the http leg.
        run: uv sync --frozen --python 3.13 --extra http

      - name: Download haybarn-unittest
        run: |
          gh release download "$HAYBARN_RELEASE" \
            --repo Query-farm-haybarn/haybarn \
            --pattern '${{ matrix.asset }}' \
            --output haybarn-unittest.zip --clobber
          mkdir -p hb && unzip -o -q haybarn-unittest.zip -d hb
        env:
          GH_TOKEN: ${{ github.token }}
          HAYBARN_RELEASE: ${{ needs.resolve-haybarn.outputs.release }}

      - name: Resolve runner + worker paths
        run: |
          # Absolute paths: run-integration.sh cd's into a staging dir before
          # invoking the runner, so relative paths would not resolve. `uv sync`
          # installs the worker into `.venv` with a `vgi-causal` console script;
          # we spawn that (absolute path) rather than `uv run causal_worker.py`
          # so the worker runs the pinned SDK from the project venv, not a
          # separately resolved PEP 723 script env. WORKER_CMD is used directly
          # as the stdio LOCATION for subprocess, and as the process the harness
          # boots for --http / --unix.
          UNITTEST="$PWD/$(find hb -name 'haybarn-unittest' -type f | head -1)"
          chmod +x "$UNITTEST"
          echo "HAYBARN_UNITTEST=$UNITTEST" >> "$GITHUB_ENV"
          echo "WORKER_CMD=$PWD/.venv/bin/vgi-causal" >> "$GITHUB_ENV"

      - name: Run extension integration suite (${{ matrix.transport }})
        run: ci/run-integration.sh
        env:
          TRANSPORT: ${{ matrix.transport }}
